# SRE — Session Log

Placeholder for now — not wired into the sre procedure yet. Intended future
use: a running record of incidents diagnosed across sessions (root cause
found, what was handed off to devops, whether the handoff was actioned), so
a new session can check whether a diagnosed issue is still awaiting a fix.
Extend this once real usage patterns make clear what's actually worth
logging here.
