# part2-tip-terraform-category

**Request:** Move to Part 2 of the pipeline demo — invoke developer, starting with a check of what application code already exists.
**Routed to:** developer
**Reason:** `inbox/requirement_app.md` Section 3 is explicitly Part 2's app-code brief (add a `terraform` category to `/tip`, `app/data/tips.js` only, no new route/file/dependency) — this is a developer task per the brief's own framing ("a `developer` change on a feature branch, PR'd directly against `main`"). Pipeline layer (Part 1) is already merged and verified; this is the change intended to exercise it for the first time.
