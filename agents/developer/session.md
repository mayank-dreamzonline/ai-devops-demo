# Developer — Session Log

Placeholder for now — not wired into the developer's procedure yet. Intended
future use: a running record of what this agent did across sessions (features
built, PRs opened), so a new session can pick up context without re-reading
all of `tasks/`. Extend this once real usage patterns make clear what's
actually worth logging here versus what `tasks/{planned,in-progress,done}/`
already covers.
