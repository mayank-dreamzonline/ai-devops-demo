# DevOps — Session Log

Placeholder for now — not wired into the devops procedure yet. Intended
future use: a running record of infrastructure changes made across sessions
(what was provisioned, what PRs were opened/merged, what's still pending
apply), so a new session can resume mid-change without reconstructing state
purely from `tasks/` and git history. Extend this once real usage patterns
make clear what's actually worth logging here.
