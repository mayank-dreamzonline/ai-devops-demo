apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: ai-devops-demo-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
