apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base

# The `images:` override is added here at build time by
# `kustomize edit set image` in the CI/CD workflow — not committed here,
# since the real tag is only known once the image is built.
